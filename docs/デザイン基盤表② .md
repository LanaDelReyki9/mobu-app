# デザイン基盤表②

| 画面名 | 主要UI要素一覧（要素名＋役割） | 使用コンポーネント（既存／新規） | 使用カラー／フォント（参照：資産トークン表の項目名） | 遷移先・挙動（簡潔なフロー） |
| --- | --- | --- | --- | --- |
| A-1. アプリ初回起動画面(ウェルカム) | ・アプリロゴ画像(画面中央配置)
・ウェルカムテキスト(簡単な挨拶)
・タップで次画面へ遷移(画面の任意の場所をタップ)

【補足】初回起動時のみ表示。瞬き演出(UI-1)は使用しない | 既存:Text
新規:WelcomeView(ロゴ + テキストの中央配置、タップで遷移) | 色:
- 背景 #F4F9F6(背景ベース)
- テキスト #3F4342(テキスト濃)

フォント:
- ウェルカムテキスト MPLUSRounded1c-Regular.ttf | 初回起動時のみ表示。画面タップでA-2(ニックネーム入力)へ遷移 |
| A-2. ニックネーム入力/初期設定 | ・タイトル(画面上部、「あなたのニックネームを教えてください」)
・ニックネーム入力フィールド(最大10文字、全角・半角問わず)
・入力制限:絵文字・特殊記号(#、@、%等)は禁止
・エラーメッセージ表示エリア(入力制限超過時、入力フィールド直下に淡い色で優しい口調で表示)
・次へボタン(入力制限を満たしている場合のみ活性化)

【補足】ニックネームの重複チェックは行わない。システムは一意のユーザーIDを自動生成し内部管理に使用(画面上には非表示) | 既存:Text, TextInput, Button
新規:NicknameInputField(最大10文字制限、絵文字・特殊記号禁止、エラーメッセージ表示機能) | 色:
- 背景 #F4F9F6(背景ベース)
- タイトル #3F4342(テキスト濃)
- 入力フィールド枠 #B9DCC6(メイン)
- エラーメッセージ #F2C9DB(アクセント/淡く)
- 次へボタン背景 #B9DCC6(メイン)

フォント:
- タイトル MPLUSRounded1c-Bold.ttf
- 入力テキスト MPLUSRounded1c-Regular.ttf
- エラーメッセージ MPLUSRounded1c-Light.ttf
- ボタンテキスト MPLUSRounded1c-Medium.ttf | A-1(ウェルカム画面)から遷移。入力制限を満たした状態で次へボタン押下 → A-3(タスク選択画面)へ遷移 |
| A-3. タスク選択画面(初期設定) | ・タイトル(画面上部、「あなたの目標を3つ選んでください」)
・カテゴリ展開リスト(アコーディオン形式、5カテゴリ)
- カテゴリ:①食生活 ②生活習慣 ③運動 ④美容 ⑤メンタル・癒し
- カテゴリチップ左側に線画イラスト配置
- カテゴリタップでタスクチップがスライド出現
・タスクチップ(合計12種類、3つまで選択可)
- 選択時:パステルカラーで塗りつぶし+右上にチェックマーク
・確定ボタン(「これで決定」、3つ選択時に活性化) | 既存:Text, Button
新規:AccordionCategoryList(アコーディオン形式カテゴリリスト、5カテゴリ × 12種類タスク、3つまで選択可能) | 色：
- 背景 #EFF4F2（背景）
- タイトル #3F4342（テキスト濃）
- カテゴリ見出し #3F4342（テキスト濃）
- タスク名 #3F4342（テキスト濃）
- 決定ボタン背景 #B9DCC6（メイン）、テキスト #3F4342

フォント：
- タイトル MPLUSRounded1c-Bold.ttf
- カテゴリ見出し MPLUSRounded1c-SemiBold.ttf
- タスク名 MPLUSRounded1c-Regular.ttf
- ボタンテキスト MPLUSRounded1c-Medium.ttf

【共通ルール通り】基本カラー・フォントを適用 | A-2(ニックネーム入力)から遷移。3つ選択後、確定ボタン押下で画面フェードアウト → C-1(カフェ/モブ君画面)へ遷移 |
| A-4. アプリ設定画面 | ・タイトル(画面上部、「設定」)
・サウンド設定(BGM/SEのON/OFF)
・通知設定
- 通知時刻:7:30/12:30/22:30の3つの固定時刻から選択制
・ニックネーム変更(テキスト入力フィールド + 保存ボタン)
・タスク変更ボタン(タップでA-3へ遷移。変更後も累計データは保持)
・ヘルプ/規約
- 利用規約へのリンク
- プライバシーポリシーへのリンク
・アカウント関連
- 退会ボタン(確認ダイアログ表示)

【補足】モブ君は表示しない(システム画面のため) | 既存:Text, Toggle, TextInput, Button, Dialog
新規:FixedTimeSelector(通知時刻選択、7:30/12:30/22:30の3択) | 色:
- 背景 #F4F9F6(背景ベース)
- タイトル #3F4342(テキスト濃)
- 設定項目テキスト #3F4342(テキスト濃)
- ボタン背景 #B9DCC6(メイン)

フォント:
- タイトル MPLUSRounded1c-Bold.ttf
- 設定項目 MPLUSRounded1c-Regular.ttf
- ボタンテキスト MPLUSRounded1c-Medium.ttf | B-1(ホーム画面)下部の歯車アイコンから遷移。設定変更後、B-1へ戻る。タスク変更時はA-3へ遷移 |
| B-1. ホーム画面(メイン画面) | ・ガイダンスメッセージ(画面中央上部、「今日完了したタスクにチェックを入れてください」、淡い色・小フォント)
・タスクチップ(3つ、縦一列表示)
- 構成:チェックボックス(左端) + タスクアイコン(線画、20px) + タスク内容
・完了ボタン(タスクチップ下部、チェック1つ以上で活性化)
・カレンダー表示(月間カレンダー)
- 達成日:お花(1輪)表示、色の濃淡で1〜3個達成を表現
- テーマ色:累計回数で自動切替(0〜9回ピンク、10〜19回イエロー、20〜29回ブルー、30〜40回ラベンダー)
- 花の種類:10回ごとに変化
・プロフィールアイコン(画面下部右側、B-3へ遷移)
・歯車アイコン(画面下部右側、A-4へ遷移) | 既存:Text, Checkbox, Button
新規:TaskChipList(タスクチップ3つ、チェックボックス + アイコン + テキスト)、MonthlyCalendar(月間カレンダー、達成日にお花表示、テーマ色・濃淡・花種類の自動管理) | 色:
- 背景 #F4F9F6(背景ベース)
- ガイダンステキスト #A5ABA9(テキスト薄)
- タスクテキスト #3F4342(テキスト濃)
- 完了ボタン背景 #B9DCC6(メイン)
- お花テーマ色:#F2C9DB(ピンク)、#FFF4D7(イエロー)、#D0E0FB(ブルー)、#E6E6FA(ラベンダー)

フォント:
- ガイダンス MPLUSRounded1c-Light.ttf
- タスク名 MPLUSRounded1c-Regular.ttf
- ボタンテキスト MPLUSRounded1c-Medium.ttf | ・初回:A-3(タスク選択)から遷移
・2回目以降:アプリ起動で直接表示
・完了ボタン押下 → 瞬き演出(UI-1) → B-2(タスク報告UI)へ遷移
・プロフィールアイコンタップ → B-3へ遷移
・歯車アイコンタップ → A-4へ遷移 |
| B-2. タスク報告UI(画面A'/A/B) | ・背景イラスト(一人称視点、スマホを持つ手元)
・画面A'(1個完了時):心のセリフ「よし、完了!モブ君に報告しよっと♪」等 → 自動でC-2へ遷移
・画面A(2個以上完了時):心のセリフ「今日は色々頑張ったけど、特に頑張った一つをモブ君に報告しよう。」等 → 画面Bへ遷移
・画面B(タスク選択):雲形吹き出しで完了タスクチップを囲んで表示。タップで即座にC-2へ遷移。心のセリフ表示なし | 既存:Text
新規:ViewpointIllustration(一人称視点背景イラスト)、ThoughtBubble(心のセリフ表示)、CloudBubbleSelection(雲形吹き出しでタスク選択肢を囲む、タップで即座に遷移) | 色:
- 心のセリフテキスト #3F4342(テキスト濃)
- 雲形吹き出し #FFFFFF(白)、境界線 #B9DCC6(メイン)

フォント:
- 心のセリフ MPLUSRounded1c-Regular.ttf
- タスク選択肢 MPLUSRounded1c-Regular.ttf | B-1(ホーム画面)で完了ボタン押下 → 瞬き演出(UI-1) → B-2(画面A'/A/B)でタスク選択 → C-2(LINE画面)へ遷移 |
| B-3. プロフィール画面(総合成果) | ・ニックネーム表示(画面上部)
・タスク総達成回数表示
・累計プレイ日数表示
・テディベアUI(4体、画面中央)
- 10回、20回、30回、40回達成を象徴
- お腹に達成回数表示(例:10 TASK)
- ミニマルUI風の線画デザイン
- ブーケを後ろ手に持ったポーズ(未達成時)
- 何も持たないポーズ(手渡し済み)
・ブーケ手渡しアニメーション(紙芝居形式)
- 10回、20回、30回達成時に再生
- B-3画面が手動で開かれたときのみ再生判定
- 複数未再生でも一度に1体分のみ再生
・進捗表示(「次のブーケまで あと X 回」)
・SNSシェアボタン(総合成果のシェア)

【補足】世界Aのみ(モブ君の要素なし)。10回目と20回目のプレゼントイベント時、C-1のセリフブロック最後に、B-3演出 | 既存:Text, Button
新規:TeddyBearDisplay(4体管理、達成回数表示、ポーズ切り替え、ブーケ手渡しアニメーション機能)、ProgressDisplay(次のブーケまでの残り回数表示)、ShareButton(SNSシェア) | 色:
- 背景 #F4F9F6(背景ベース)
- テキスト #3F4342(テキスト濃)
- テディベア・ブーケ:テーマ色(#F2C9DB、#FFF4D7、#D0E0FB、#E6E6FA)
- シェアボタン背景 #B9DCC6(メイン)

フォント:
- ニックネーム MPLUSRounded1c-Bold.ttf
- 達成回数・日数 MPLUSRounded1c-SemiBold.ttf
- 進捗テキスト MPLUSRounded1c-Regular.ttf
- ボタンテキスト MPLUSRounded1c-Medium.ttf | B-1(ホーム画面)下部のプロフィールアイコンから遷移。閲覧後、B-1へ戻る。10回目と20回目のプレゼントイベント時、C-1のセリフでB-3への誘導あり(演出未再生時のみ) |
| C-1. カフェ/モブ君画面(対面) | ・背景(カフェ店内、固定)
・モブ君立ち絵(画面左側、カウンター越しの一人称視点アングル)
- Ver.1〜4で共有アングル、ポーズ・表情で成長を表現
- 表情アセット(各Ver.のセリフブロックに対応、特に③の真剣な眼差し・焦り)
・セリフ表示エリア(画面下部、吹き出しなし)
- タスクごとに専用セリフを表示
- セリフ枠とに立ち絵の表示に特化
・イベント専用アセット(立ち絵が消えた後)
- 例:手の差し出しイラスト(40回告白時)

【補足】③初回タスク報告時のみ、「あ、俺用事あるの忘れてた!」というセリフと共にモブ君が物理的に離脱する(背を向けるなどの紙芝居演出)。プレゼントの箱の画像や特別な獲得演出といった固有の新しいUI要素は必要としない | 既存:Text
新規:CafeSceneView(背景 + 立ち絵 + セリフ表示の統合コンポーネント、Ver.1〜4対応、表情切り替え機能、イベント専用アセット表示機能) | 色:
- セリフテキスト #3F4342(テキスト濃)

フォント:
- セリフ MPLUSRounded1c-Regular.ttf | タスク報告完了後、B-2(画面A'/A/B)から瞬き演出(UI-1)を経て遷移。イベント時(10回、20回、30回、40回達成)はC-2のメッセージブロック完了後、「カフェへ向かう」ボタンからも遷移。セリフ完了後、C-2(LINE画面)へ遷移(10回、20回達成時はC-3経由) |
| C-2. LINEリスト/チャット画面 | ・モブ君のLINEアイコン(プロフィール画像)
・トーク画面(LINE風UI、吹き出しアニメーション方式)
- モブ君のセリフ(左寄せ、白背景吹き出し)
- ユーザーのスタンプ(右寄せ、吹き出し背景なし)
・タスク反応セリフ(88種類、選択したタスクに応じて表示)
・気分共有UI(ランダム50%で発生)
- 問いかけセリフ(24パターン)
- 入力エリアがスタンプ選択UIに自動切り替え
- LINE入力バー書割UI(タップ不可)
- スタンプエリア背景(白背景)
- 6種類の気分スタンプ(静か/元気/頑張った/しんどい/不安/嬉しい)を2行表示
- 誘導テキストなし
- ユーザーがスタンプタップ → ユーザー側の発言として送信(吹き出し背景なし)
- モブ君の反応セリフ(24パターン:6種類×4段階)
・イベント専用メッセージブロック(10回、20回、30回、40回達成時)
- メッセージA、B、C(自動送り方式)
- 最終ブロック表示完了後、「カフェへ向かう」ボタンを画面最下部に表示
・オネェ化アイコン(UI-3)、バブル色(UI-5)、内心表示(UI-6)など、II-7で確定したロジックを適用

【補足】メッセージ読了後、OS標準操作(戻るボタン、スワイプ)でB-1へ遷移 | 既存:Text, Button
新規:LINEChatView(トーク画面、吹き出しアニメーション、左右寄せ管理)、MoodStampSelector(気分スタンプ選択UI、6種類2行表示、入力バー書割UI、スタンプエリア背景)、EventMessageBlock(イベント専用メッセージ、自動送り、「カフェへ向かう」ボタン表示機能) | 色:
- 背景(トーク画面) #F4F9F6または淡いブルー(青空)
- モブ君の吹き出し #FFFFFF(白)
- セリフテキスト #3F4342(テキスト濃)
- スタンプエリア背景 #FFFFFF(白)
- 「カフェへ向かう」ボタン背景 #B9DCC6(メイン)

フォント:
- セリフ MPLUSRounded1c-Regular.ttf
- ボタンテキスト MPLUSRounded1c-Medium.ttf | C-1(カフェ/モブ君画面)から遷移。通常タスク報告時は、反応セリフ表示後(気分共有発生時はその後)、OS標準操作でB-1へ戻る。イベント時(10回、20回、30回、40回達成)は、メッセージブロック完了後「カフェへ向かう」ボタン表示 → ボタンタップでC-1へ遷移 |
| C-3. プレゼント獲得画面 | ・プレゼントアイテム画像(画面中央、目立つ配置)
- 10回:試作品スイーツ
- 20回:珍しい茶葉
- 30回:選んだペン
・アイテム名表示(オーバーレイ)
・モブ君からの一言セリフ(オーバーレイ、獲得時のメッセージ)
・背景(シンプルな汎用背景またはプレゼント獲得演出用専用背景)

【補足】モブ君の立ち絵は表示しない。アイテムの提示に集中させる設計。40回達成時は、C-1からC-3をスキップし、D-1へ遷移 | 既存:Text
新規:GiftRevealView(アイテム画像 + アイテム名 + セリフのオーバーレイ表示、シンプルな獲得演出) | 色:
- 背景(シンプルな淡色または専用背景)
- アイテム名テキスト #3F4342(テキスト濃)
- セリフテキスト #3F4342(テキスト濃)

フォント:
- アイテム名 MPLUSRounded1c-SemiBold.ttf
- セリフ MPLUSRounded1c-Regular.ttf | C-1(カフェ/モブ君画面)のセリフブロック途中から自動遷移。プレゼント獲得演出完了後、自動的にC-1の最後のセリフブロックへ戻る(10回達成時はセリフブロック④、20回達成時はセリフブロック⑤) |
| C-4. プレゼント見返し画面 | ・背景(世界Bのイラストを色味を残したレトロセピア調に加工)
- 獲得済みプレゼントの「象徴的な場所」(冷蔵庫等)を配置
- モブ君の立ち絵・バストアップは初期画面に表示しない
・操作可能な場所(獲得済みのみ)
- 控えめに光らせる演出
- 控えめな指カーソルアニメーション
- タップでプレゼント詳細を拡大表示
・プレゼント詳細表示(タップ時)
- プレゼントのイラスト拡大表示(C-3で使用したアセットを再利用)
- モブ君のバストアップ(既存アセット再利用)
- プレゼント時のセリフ
・未達成の場所は光らず、タップしても反応しない

【補足】隠しコマンド:C-2(LINE画面)のモブ君のプロフィール画像を3回連続タップで遷移。周知はアプリストアの画像・動画での匂わせ(アプリ内での明示は行わない) | 既存:なし
新規:SepiaSceneView(セピア調背景 + 象徴的な場所の配置)、InteractiveSpotDisplay(光る演出 + 指カーソルアニメーション、獲得済み判定)、GiftDetailModal(拡大表示:アイテム + モブ君のバストアップ + セリフ) | 色:
- 背景(レトロセピア調、色味残し)
- 光る演出(控えめな発光、淡い #FFF4D7または #F2C9DB)
- セリフテキスト #3F4342(テキスト濃)

フォント:
- セリフ MPLUSRounded1c-Regular.ttf | C-2(LINE画面)のモブ君のプロフィール画像を3回連続タップで遷移(隠しコマンド)。閲覧後、OS標準操作でC-2またはB-1へ戻る |
| D-1. エンディング画面 | ・背景(夕日をバックにした特別な場所の風景)
・モブ君イラスト(Ver.4、告白時の表情、全身またはバストアップ)
・セリフ表示エリア(画面下部、吹き出しなし)
- セリフは3つのブロックに分割
- TAP 2、TAP 3で強制タップ進行
- 最後のセリフ完了後、任意の場所タップでB-1へ遷移
・没入感優先のため、セリフ枠以外のUI要素を最小限に抑える

【補足】C-1からTAP 1後の暗転演出を経て自動遷移。他のイベントUIへの遷移ロジックは持たない | 既存:Text
新規:ConfessionScene(背景 + モブ君イラスト + セリフ表示の統合コンポーネント、強制タップ進行、3ブロック構成) | 色:
- セリフテキスト #3F4342(テキスト濃)

フォント:
- セリフ MPLUSRounded1c-Regular.ttf | 40回タスク達成イベントのC-1画面から、TAP 1後の暗転演出を経て自動遷移 → D-1表示(TAP 2、TAP 3で進行) → 最後のセリフ完了後、任意タップでB-1(ホーム画面)へ遷移 |
| D-2. エピローグ画面 | ・背景(モブ君の自室、一人称視点)
- 机のクローズアップイラスト
- 机のテクスチャ:落ち着いた色の木目調
- 光のトーン:柔らかい午後の暖色系
・モブ君のボイスモノローグ(音声のみ、画面上にテキスト表示なし)
・効果音(環境音等)

【補足】D-1(エンディング)終了後、自動的にD-2へ遷移。モノローグ完了後、ユーザー操作なしに自動的にD-3(スタッフロール)へ遷移。BGMは使用しない(効果音とボイスに集中)。背景イラストは「分割合成」ロジックで制作 | 既存:なし
新規:EpilogueScene(一人称視点背景 + ボイスモノローグ再生、自動遷移機能) | 色:
- 背景(落ち着いた木目調、柔らかい午後の暖色系)

フォント:
- (テキスト表示なし) | D-1(エンディング)終了後、自動的にD-2へ遷移。モブ君のボイスモノローグ完全終了後、ユーザー操作なしに自動的にD-3(スタッフロール)へ遷移 |
| D-3. スタッフロール | ・縦スクロール(下から上への自動スクロール、ユーザー操作不可)
・背景演出(モブ君Ver.1〜Ver.4のイラストを使い回し)
- 画面の一部(例:3分の1)に縮小し、淡いトーンで表示
- 文字が読めるよう配慮
- 各イラストを最低3秒間表示(合計12秒以上)
・スクロール速度:極端に遅く設定
・表示項目(案)
- 企画・開発(なつき)
- 制作理念
- AI協力(Claude, ChatGPT, Geminiなど)
- Special Thanks(ユーザー)
・静止演出(スタッフロール終了後)
- Ver.4モブ君の静止画のまま数秒間(例:5秒)静止
- 小さく淡いトーンで「Fin」をフェードイン表示
・「今度はどんな私に会えるかな?」ボタン
- 静止後、画面中央にフェードイン表示
- タップ後、「瞬き2回」演出を挟んでB-1(ホーム画面)へ遷移
- 遷移完了後、世界Aのタスクリストをリセットし、初期状態(2周目)に戻す

【補足】D-2(エピローグ)終了後、自動的にD-3へ遷移。BGMはD-2からの継続使用。登場人物や世界Bのアイテムなど、ユーザーの没入感を冷ますメタ要素となる情報は記載しない | 既存:Button
新規:StaffRollView(縦スクロール、背景イラスト表示・切り替え・淡色処理、スクロール速度管理)、FinDisplay(「Fin」テキストのフェードイン表示)、RestartButton(「今度はどんな私に会えるかな?」ボタン、瞬き2回演出、タスクリセット機能) | 色:
- テキスト #3F4342(テキスト濃)
- 背景イラスト(淡いトーン処理)
- 「Fin」テキスト #A5ABA9(テキスト薄)
- ボタン背景 #B9DCC6(メイン)

フォント:
- スタッフロールテキスト MPLUSRounded1c-Regular.ttf
- 「Fin」テキスト MPLUSRounded1c-Light.ttf
- ボタンテキスト MPLUSRounded1c-Medium.ttf | D-2(エピローグ)終了後、自動的にD-3へ遷移。スタッフロール終了後、Ver.4モブ君の静止画で数秒静止 → 「Fin」表示 → 「今度はどんな私に会えるかな?」ボタン表示 → ボタンタップで「瞬き2回」演出 → B-1(ホーム画面/2周目)へ遷移、タスクリストをリセット |